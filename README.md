# Daily sync with firebase (ReactJS) with Server side rendering (with Firebase)

## install packages
`npm install`

## start server
`npm run dev`

## RUN Server
`npm start`

## webpack build
`npm run build`

## Firebase init
`firebase init`

## Firebase Deploy
`firebase deploy`